    
 <div id="page-wrapper">
        
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">Barganhaki</h1>

                        <% if notice %>
                        <div class="alert alert-success">
                                <%= notice %> <a href="#" class="alert-link">Alert Link</a>.
                            </div>
                    <% end %>
                    <% if @category.errors.any? %>
                            <% @category.errors.full_messages.each do |message| %>
                        <div class="alert alert-danger">
                                <%= message %>
                            
                              <% end %>
                         </div>
                        <% end  %>
                         <div class="panel panel-default">
                        <div class="panel-heading">
                            Nova Categoria
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                    <thead>
                                    <%= form_for [:backoffice, @category], html: {multpart: true}  do |f| %>
                                       <div class="col-lg-6">
                                        <label>Descrição</label>
                                            <div class="form-group">
                                                <%= f.text_field :description, class: "form-control", placeholder: "Ex: Pizzaria"%>      
                                            </div>
                                            <div class="form-group">
                                                <%= f.file_field :icon, class: "form-control", placeholder: "Ex: Pizzaria"%>      
                                            </div>
                                            <%= f.submit "Salvar" %>
                                        </div></br>
                                           
                                    <% end %>
                                      
                                    </thead>
                                    <tbody>
                                    
                                    </tbody>
                                </table>
                            </div>
                            <!-- /.table-responsive -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-6 -->
                    
                    </div>
                    <!-- /.col-lg-12 -->
                </div>
                <!-- /.row -->
            </div>
            <!-- /.container-fluid -->
        </div>
        <!-- /#page-wrapper -->
