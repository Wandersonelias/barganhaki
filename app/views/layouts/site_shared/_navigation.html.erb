<div class="nav navbar-default menu-user">
    <% if user_signed_in? %>
    <div class="pull-right">
        <b><%= current_user.email %></b>
              
                <%= link_to "Perfil",site_prefil_dashboard_index_path , class: "btn btn-success" %>
                <%= link_to "Sair", destroy_user_session_path, class: "btn btn-success", method: :delete %>
                
         </div>
         
    <% else %>
    <div class="pull-right">
                <%= link_to "Entrar", user_session_path, class: "btn btn-primary" %>
                <%= link_to "Cadastrar", new_user_registration_path, class: "btn btn-primary" %>
    </div>
    <% end %>
</div>

 <div class="cabecalho">
    <div class="container text-center">
        <%= image_tag("logo.png") %>
    </div>
  </div>
  
    <div class="cat">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        
        
                <div class="collapse navbar-collapse cat" id="navbarResponsive">
                <div class="container">
                    <ul class="navbar-nav ml-auto lista">
                        <% @categories.each do |categorias| %>                   
                        <li class="nav-item active text-center">
                            <div class="circle">
                            <span class="icon-tt text-justify"><%= image_tag categorias.icon.url(:thumb) %></span></br>
                            </div>
                        <a class="nav-link" href="#"><%= categorias.description %></a>
                        </li>
                        <% end %>
                    </ul>
                </div>    
                
        </nav>
    </div>
    
    